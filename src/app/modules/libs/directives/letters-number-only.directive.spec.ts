import { LettersNumberOnlyDirective } from './letters-number-only.directive';

describe('LettersNumberOnlyDirective', () => {
  it('should create an instance', () => {
    const directive = new LettersNumberOnlyDirective();
    expect(directive).toBeTruthy();
  });
});
